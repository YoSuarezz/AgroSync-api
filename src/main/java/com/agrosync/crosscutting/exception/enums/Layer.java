package com.agrosync.crosscutting.exception.enums;

public enum Layer {
    GENERAL, APPLICATION, DOMAIN, DTO, ENTITY, USECASE, RULE, REPOSITORY,DATA,
    CONTROLLER, BUSINESS, INITIALIZER, CROSSCUTTING, DEFAULT
}
